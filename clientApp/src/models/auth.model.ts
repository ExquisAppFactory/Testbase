export interface LoginModel {
    username: string;
    password: string;
}

export interface UserRegisterModel {
    firstName: string;
    lastName: string;
    username: string;
    password: string;
}